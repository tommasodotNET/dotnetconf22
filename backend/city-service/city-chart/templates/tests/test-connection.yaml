apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "city-chart.fullname" . }}-test-connection"
  labels:
    {{- include "city-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "city-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
